/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['miro.medium.com', 'upload.wikimedia.org', 'images.unsplash.com', 'plus.unsplash.com'],
      },
};

export default nextConfig;
